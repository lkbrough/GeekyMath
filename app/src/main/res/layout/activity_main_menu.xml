<!--So this is the XML document! Hopefully this is the first one you looking at so you could be reading this...-->
<!--Anyway this first few set of lines in this Linear Layout Element should just be left alone but if you're curious I will explain it-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <LinearLayout
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:background="#0000ff"
        tools:context=".MainMenu">
        <!--The lines labeled layout: These two lines are just setting the size of the viewing area to the screen size... Pretty simple right?-->
        <!--The lines labeled padding: These lines are setting padding... Really don't worry about this...-->
        <!--Remove the vertical Screen lock so the user can rotate their screen when they want 1/22/2016-->

        <!--This the first button on the screen! It gives the option for the user to learn and is are wide as the screen and as tall as it needs to be-->
        <Button android:id="@+id/learn"
            android:text="@string/learn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="#00ff00"
            android:background="#000000"
            android:onClick="load"
            />

        <Button android:id="@+id/test10"
            android:text="@string/test10"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="#00ff00"
            android:background="#000000"
            android:onClick="billy"
            />

        <Button android:text="@string/opt"
            android:id="@+id/opt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="#00ff00"
            android:background="#000000"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/contact"
            android:textColor="#00ff00"
            android:textSize="18sp"
            />

    </LinearLayout>
</ScrollView>
